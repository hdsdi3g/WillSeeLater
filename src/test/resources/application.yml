# Don't forget to update src/main/resources/META-INF/additional-spring-configuration-metadata.json
# after to adds non-official entries

spring:
    main:
        banner-mode: "off"
        log-startup-info: false
server:
    address: 127.0.0.1
    port: 8080

willseelater:
    cacheDir: .cache
    workingDir: target/temp-working
